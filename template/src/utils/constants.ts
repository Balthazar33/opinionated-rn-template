import {sizer} from './metrics';

export const SCREEN_PADDING = sizer(16);

export const SPLASH_HIDE_DURATION = 3000; // The duration for which the splash screen will be displayed; adjust as needed.

// TODO: Add custom fonts
export enum Fonts {
  BOLD = 'roboto',
  MEDIUM = 'roboto',
  REGULAR = 'roboto',
}
